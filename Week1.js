function calculate(){
        var input = $('#expression'); //get the box
        var val = input.val(); //get the value inside the box
        console.log(val.toUpperCase()); //log the value to the console
    }
        